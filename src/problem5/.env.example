# Postgres connection string used by Prisma and the NestJS app.
# Matches the docker-compose.yml service defaults.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tasks?schema=public"

# HTTP port the NestJS server listens on.
PORT=3000
